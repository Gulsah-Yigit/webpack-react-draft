import { createSlice } from "@reduxjs/toolkit";

const appState = createSlice({
  name: "appState",
  initialState: {},
  reducers: {},
});

export const {} = appState.actions;
export default appState.reducer;
